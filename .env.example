# Database (port 5433 to avoid conflict with local postgres)
DATABASE_URL=postgresql://smartdorm:smartdorm123@localhost:5433/smartdorm

# JWT Secret (change in production!)
JWT_SECRET=smartdorm-dev-secret-key-change-in-production

# For Neon (production) - replace with your Neon connection string
# DATABASE_URL=postgresql://user:password@ep-xxxxx.ap-southeast-1.aws.neon.tech/smartdorm?sslmode=require
